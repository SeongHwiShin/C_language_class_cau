#include <stdio.h>
#include <math.h>

int main() {

    /*
    int a=4;
    int b=22;
    int tmp;

    tmp=a;
    a=b;
    b=tmp;
    printf("a=%d, b=%d\n", a,b);

    return 0;
    */

    /*
    float fvalue=1234567890.12345678901234567890;
    double dvalue=1234567890.12345678901234567890;
    printf("float형 변수%30.25f\n", fvalue);
    printf("double형 변수%30.25lf\n", dvalue);
    */
/*
    float x=1e39;
    float y=1.23456e-46;

    printf("x=%e\n", x);
    printf("y=%e\n", y);
*/
/*
    double light_speed = 300000;
    double distance = 149600000;
    double time;

    time = distance/light_speed;
    time = time/60.0;

    printf("빛의 속도는 %fkm/s \n", light_speed);
    printf("태양과 지구의 거리 %fkm \n", distance);
    printf("도달시간은 %f분\n", time);
*/
/*
    double celsius, fahrenfeit;

    printf("화씨온도 ");
    scanf("%lf", &fahrenfeit);
    celsius=(fahrenfeit-32.0)*5.0/9.0;
    printf("섭씨온도 = %lf\n", celsius);
*/
/*
    double radius;
    printf("반지름 입력 ");
    scanf("%lf",&radius );
    printf("면적 = %lf", radius*radius*3.14);
 */
/*
    char c;
    printf("문자입력 : ");
    scanf("%c", &c);
    printf("입력한문자는 %c", c);
    return 0;
 */
    /*
    char ch='A';
    printf("%c의 아스키코드=%d\n", ch, ch);
    ch='B';
    printf("%c의 아스키코드=%d\n", ch, ch);
    */

    /*
    printf("%c\n",'s');
    printf("%d\n",'s');
    printf("%d\n",4);
    printf("%c\n",'4');
    printf("%d",'4');
    return 0;
    */
    /*
    printf("%c, %s \n", 'a', "A");
    */

    /*
    int id, pw;

    printf("아이디와 패스워드를 4개의 숫자로 입력하세요 ");
    printf("id:____\b\b\b\b");
    scanf("%d", &id);
    printf("pw:____\b\b\b\b");
    scanf("%d", &pw);
    printf("입력된 아이디는 \"%d\"이고 패스워드는 \"%d\"입니다",id, pw);

    return 0;
    */

    /*
    printf("char형의 크기는 %d바이트입니다\n", sizeof(char));
    printf("short형의 크기는 %d바이트입니다\n", sizeof(short));
    printf("int형의 크기는 %d바이트입니다\n", sizeof(int));
    printf("long형의 크기는 %d바이트입니다\n", sizeof(long));
    printf("long long형의 크기는 %d바이트입니다\n", sizeof(long));
    printf("float형의 크기는 %d바이트입니다\n", sizeof(long long));
    printf("double형의 크기는 %d바이트입니다\n", sizeof(float));
    printf("double형의 크기는 %d바이트입니다\n", sizeof(double));
    printf("long double형의 크기는 %d바이트입니다\n", sizeof(long double));
    return 0;
    */
    /*
    int x=1;
    printf("3+2의 값은 %d\n", 3+2);
    printf("3-2의 값은 %d\n", 3-2);
    printf("x의 값은 %d\n", x);
    printf("x+1의 값은 %d\n", x+1);
    */
    /*
    int x, y, result;
    printf("두개의 정수 입력 ");
    scanf("%d %d", &x, &y);

    result = x+y;
    printf("%d + %d = %d", x, y, result);
    result = x-y;
    printf("%d - %d = %d", x, y, result);
    result = x*y;
    printf("%d * %d = %d", x, y, result);
    result = x/y;
    printf("%d / %d = %d", x, y, result);
    result = x%y;
    printf("%d %% %d = %d", x, y, result);
    */

    /*
    double x1, x2, a, b, c, d;

    a=1.0;
    b=3.0;
    c=2.0;
    d=b*b-4.0*a*c;
    x1=(-b + sqrt(d))/(2.0*a);
    x2=(-b - sqrt(d))/(2.0*a);
    printf("x1= %f\nx2=%f\n", x1, x2);
    return 0;
    */
/*
#define SEC_PER_MINUTE 60
    int input, minute, second;

    printf("초단위의 시간입력 ");
    scanf("%d", &input);
    minute=input/SEC_PER_MINUTE;
    second=input%SEC_PER_MINUTE;
    printf("%d초는 %d분 %d초 입니다", input, minute, second);
    return 0;
*/
    
}
